
var IDENTITY = ID = I = x => x

IDENTITY(1) // --> 1

IDENTITY(IDENTITY) // --> IDENTITY

(x => x)(x => x) // --> x => x

////////////////////////////////////////

var ADD_1 = x => x + 1

ADD_1(12) // --> 13

(x => y => x + y)(1)(2) // --> 3

(x => y => x + y)(1) // --> ADD_1

///////////////////////////////////////

var SELF-APPLY = S = s => s(s)

S(I) // --> I

S(S) // S(S)

///////////////////////////////////////